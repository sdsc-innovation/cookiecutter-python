from setuptools import find_packages, setup

setup(
    name="src",
    packages=find_packages(),
    version="0.1.0",
    python_requires=">=3.7",
    install_requires=[],  # TODO installation requirements
)
